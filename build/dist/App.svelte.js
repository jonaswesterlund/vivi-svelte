/* src\App.svelte generated by Svelte v3.37.0 */
import {
	SvelteComponent,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "../_snowpack/pkg/svelte/internal.js";

import Main from "./_components/Main.svelte.js";
import axios from "../_snowpack/pkg/axios.js";
import { categories, questions } from "./stores.js";
import Tailwindcss from "./Tailwindcss.svelte.js";

function create_fragment(ctx) {
	let tailwindcss;
	let t0;
	let nav;
	let t2;
	let div2;
	let main;
	let current;
	tailwindcss = new Tailwindcss({});
	main = new Main({});

	return {
		c() {
			create_component(tailwindcss.$$.fragment);
			t0 = space();
			nav = element("nav");

			nav.innerHTML = `<ul class="flex border-b-2 border-blue-600"><div class="flex-1 mr-2 mb-4"><div class="text-center block text-blue-600 text-xl font-semibold
          hover:text-blue-800">Vivi</div></div></ul>`;

			t2 = space();
			div2 = element("div");
			create_component(main.$$.fragment);
			attr(nav, "class", "my-4");
			attr(div2, "class", "container mx-auto px-4 py-4");
		},
		m(target, anchor) {
			mount_component(tailwindcss, target, anchor);
			insert(target, t0, anchor);
			insert(target, nav, anchor);
			insert(target, t2, anchor);
			insert(target, div2, anchor);
			mount_component(main, div2, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(tailwindcss.$$.fragment, local);
			transition_in(main.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(tailwindcss.$$.fragment, local);
			transition_out(main.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(tailwindcss, detaching);
			if (detaching) detach(t0);
			if (detaching) detach(nav);
			if (detaching) detach(t2);
			if (detaching) detach(div2);
			destroy_component(main);
		}
	};
}

function instance($$self) {
	Promise.all([
		axios.get("http://localhost:8081/api/questions"),
		axios.get("http://localhost:8081/api/categories")
	]).then(([questionsResponse, categoriesResponse]) => {
		questions.set(questionsResponse.data);
		categories.set(categoriesResponse.data);
	});

	return [];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default App;