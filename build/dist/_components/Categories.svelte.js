import './Categories.svelte.css.proxy.js';
/* src\components\Categories.svelte generated by Svelte v3.37.0 */
import {
	SvelteComponent,
	append,
	attr,
	component_subscribe,
	destroy_each,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	set_data,
	set_store_value,
	space,
	text,
	toggle_class
} from "../../_snowpack/pkg/svelte/internal.js";

import { categories, selectedCategories } from "../stores.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[4] = list[i];
	return child_ctx;
}

// (23:2) {#each $categories as category}
function create_each_block(ctx) {
	let button;
	let t0_value = /*category*/ ctx[4].name + "";
	let t0;
	let t1;
	let mounted;
	let dispose;

	function click_handler() {
		return /*click_handler*/ ctx[3](/*category*/ ctx[4]);
	}

	return {
		c() {
			button = element("button");
			t0 = text(t0_value);
			t1 = space();
			attr(button, "class", "border-blue-600 rounded-full border-2 px-4 py-1 mr-2\r\n      focus:outline-none svelte-1bs6wu2");
			toggle_class(button, "active", /*$selectedCategories*/ ctx[0].map(func).includes(/*category*/ ctx[4].id));
		},
		m(target, anchor) {
			insert(target, button, anchor);
			append(button, t0);
			append(button, t1);

			if (!mounted) {
				dispose = listen(button, "click", click_handler);
				mounted = true;
			}
		},
		p(new_ctx, dirty) {
			ctx = new_ctx;
			if (dirty & /*$categories*/ 2 && t0_value !== (t0_value = /*category*/ ctx[4].name + "")) set_data(t0, t0_value);

			if (dirty & /*$selectedCategories, $categories*/ 3) {
				toggle_class(button, "active", /*$selectedCategories*/ ctx[0].map(func).includes(/*category*/ ctx[4].id));
			}
		},
		d(detaching) {
			if (detaching) detach(button);
			mounted = false;
			dispose();
		}
	};
}

function create_fragment(ctx) {
	let div;
	let each_value = /*$categories*/ ctx[1];
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	return {
		c() {
			div = element("div");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			attr(div, "class", "flex justify-center");
		},
		m(target, anchor) {
			insert(target, div, anchor);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(div, null);
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*$selectedCategories, $categories, toggleCategory*/ 7) {
				each_value = /*$categories*/ ctx[1];
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(div, null);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value.length;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			destroy_each(each_blocks, detaching);
		}
	};
}

const func = c => c.id;

function instance($$self, $$props, $$invalidate) {
	let $selectedCategories;
	let $categories;
	component_subscribe($$self, selectedCategories, $$value => $$invalidate(0, $selectedCategories = $$value));
	component_subscribe($$self, categories, $$value => $$invalidate(1, $categories = $$value));

	const toggleCategory = category => {
		if ($selectedCategories.map(c => c.id).includes(category.id)) {
			set_store_value(selectedCategories, $selectedCategories = $selectedCategories.filter(c => c.id !== category.id), $selectedCategories);
		} else {
			set_store_value(selectedCategories, $selectedCategories = [...$selectedCategories, category], $selectedCategories);
		}
	};

	const click_handler = category => toggleCategory(category);
	return [$selectedCategories, $categories, toggleCategory, click_handler];
}

class Categories extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Categories;