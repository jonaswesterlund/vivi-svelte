/* src\_components\Question.svelte generated by Svelte v3.37.0 */
import {
	SvelteComponent,
	append,
	attr,
	check_outros,
	component_subscribe,
	create_component,
	destroy_component,
	destroy_each,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	listen,
	mount_component,
	safe_not_equal,
	set_data,
	space,
	text,
	transition_in,
	transition_out
} from "../../_snowpack/pkg/svelte/internal.js";

import axios from "../../_snowpack/pkg/axios.js";
import AnswerEvaluation from "./AnswerEvaluation.svelte.js";
import { selectedQuestion } from "../stores.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[5] = list[i];
	return child_ctx;
}

function get_each_context_1(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[8] = list[i];
	return child_ctx;
}

// (33:0) {#if $selectedQuestion}
function create_if_block(ctx) {
	let div4;
	let div0;
	let p;
	let t0_value = /*$selectedQuestion*/ ctx[0].content + "";
	let t0;
	let t1;
	let div1;
	let t2;
	let div2;
	let t3;
	let div3;
	let answerevaluation;
	let current;
	let each_value_1 = /*$selectedQuestion*/ ctx[0].categories;
	let each_blocks_1 = [];

	for (let i = 0; i < each_value_1.length; i += 1) {
		each_blocks_1[i] = create_each_block_1(get_each_context_1(ctx, each_value_1, i));
	}

	let each_value = /*$selectedQuestion*/ ctx[0].answerChoices;
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	answerevaluation = new AnswerEvaluation({
			props: {
				answerEvaluation: /*answerEvaluation*/ ctx[1]
			}
		});

	return {
		c() {
			div4 = element("div");
			div0 = element("div");
			p = element("p");
			t0 = text(t0_value);
			t1 = space();
			div1 = element("div");

			for (let i = 0; i < each_blocks_1.length; i += 1) {
				each_blocks_1[i].c();
			}

			t2 = space();
			div2 = element("div");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t3 = space();
			div3 = element("div");
			create_component(answerevaluation.$$.fragment);
			attr(p, "class", "text-xl font-semibold");
			attr(div0, "class", "flex mt-8");
			attr(div1, "class", "absolute top-0 right-0 mt-2 mr-2");
			attr(div2, "class", "flex flex-wrap justify-between mx-4 mt-12");
			attr(div3, "class", "flex justify-center");
			attr(div4, "class", "border-2 border-gray-600 p-4 w-1/2 mx-auto relative shadow-xl");
		},
		m(target, anchor) {
			insert(target, div4, anchor);
			append(div4, div0);
			append(div0, p);
			append(p, t0);
			append(div4, t1);
			append(div4, div1);

			for (let i = 0; i < each_blocks_1.length; i += 1) {
				each_blocks_1[i].m(div1, null);
			}

			append(div4, t2);
			append(div4, div2);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(div2, null);
			}

			append(div4, t3);
			append(div4, div3);
			mount_component(answerevaluation, div3, null);
			current = true;
		},
		p(ctx, dirty) {
			if ((!current || dirty & /*$selectedQuestion*/ 1) && t0_value !== (t0_value = /*$selectedQuestion*/ ctx[0].content + "")) set_data(t0, t0_value);

			if (dirty & /*$selectedQuestion*/ 1) {
				each_value_1 = /*$selectedQuestion*/ ctx[0].categories;
				let i;

				for (i = 0; i < each_value_1.length; i += 1) {
					const child_ctx = get_each_context_1(ctx, each_value_1, i);

					if (each_blocks_1[i]) {
						each_blocks_1[i].p(child_ctx, dirty);
					} else {
						each_blocks_1[i] = create_each_block_1(child_ctx);
						each_blocks_1[i].c();
						each_blocks_1[i].m(div1, null);
					}
				}

				for (; i < each_blocks_1.length; i += 1) {
					each_blocks_1[i].d(1);
				}

				each_blocks_1.length = each_value_1.length;
			}

			if (dirty & /*addAnswer, $selectedQuestion*/ 5) {
				each_value = /*$selectedQuestion*/ ctx[0].answerChoices;
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(div2, null);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value.length;
			}

			const answerevaluation_changes = {};
			if (dirty & /*answerEvaluation*/ 2) answerevaluation_changes.answerEvaluation = /*answerEvaluation*/ ctx[1];
			answerevaluation.$set(answerevaluation_changes);
		},
		i(local) {
			if (current) return;
			transition_in(answerevaluation.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(answerevaluation.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div4);
			destroy_each(each_blocks_1, detaching);
			destroy_each(each_blocks, detaching);
			destroy_component(answerevaluation);
		}
	};
}

// (41:6) {#each $selectedQuestion.categories as category}
function create_each_block_1(ctx) {
	let span;
	let t_value = /*category*/ ctx[8].name + "";
	let t;

	return {
		c() {
			span = element("span");
			t = text(t_value);
			attr(span, "class", "mr-4 italic text-gray-500 text-sm");
		},
		m(target, anchor) {
			insert(target, span, anchor);
			append(span, t);
		},
		p(ctx, dirty) {
			if (dirty & /*$selectedQuestion*/ 1 && t_value !== (t_value = /*category*/ ctx[8].name + "")) set_data(t, t_value);
		},
		d(detaching) {
			if (detaching) detach(span);
		}
	};
}

// (47:6) {#each $selectedQuestion.answerChoices as choice}
function create_each_block(ctx) {
	let div;
	let button;
	let t0_value = /*choice*/ ctx[5].answer + "";
	let t0;
	let t1;
	let mounted;
	let dispose;

	function click_handler() {
		return /*click_handler*/ ctx[3](/*choice*/ ctx[5]);
	}

	return {
		c() {
			div = element("div");
			button = element("button");
			t0 = text(t0_value);
			t1 = space();
			attr(button, "class", "w-full border-2 border-blue-600 p-2 hover:bg-blue-600\r\n            hover:text-white rounded");
			attr(div, "class", "w-1/3 mb-4 mx-4");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, button);
			append(button, t0);
			append(div, t1);

			if (!mounted) {
				dispose = listen(button, "click", click_handler);
				mounted = true;
			}
		},
		p(new_ctx, dirty) {
			ctx = new_ctx;
			if (dirty & /*$selectedQuestion*/ 1 && t0_value !== (t0_value = /*choice*/ ctx[5].answer + "")) set_data(t0, t0_value);
		},
		d(detaching) {
			if (detaching) detach(div);
			mounted = false;
			dispose();
		}
	};
}

function create_fragment(ctx) {
	let if_block_anchor;
	let current;
	let if_block = /*$selectedQuestion*/ ctx[0] && create_if_block(ctx);

	return {
		c() {
			if (if_block) if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if (if_block) if_block.m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			if (/*$selectedQuestion*/ ctx[0]) {
				if (if_block) {
					if_block.p(ctx, dirty);

					if (dirty & /*$selectedQuestion*/ 1) {
						transition_in(if_block, 1);
					}
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					transition_in(if_block, 1);
					if_block.m(if_block_anchor.parentNode, if_block_anchor);
				}
			} else if (if_block) {
				group_outros();

				transition_out(if_block, 1, 1, () => {
					if_block = null;
				});

				check_outros();
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (if_block) if_block.d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let $selectedQuestion;
	component_subscribe($$self, selectedQuestion, $$value => $$invalidate(0, $selectedQuestion = $$value));
	let firstAnswer = true;
	let answerEvaluation;

	const addAnswer = async id => {
		const response = await axios.post("http://localhost:8081/api/answers", {
			questionId: $selectedQuestion.id,
			answerChoiceId: id,
			firstAnswer
		});

		firstAnswer = false;
		const questionEvaluation = response.data;
		const isCorrect = questionEvaluation.correctAnswerChoice.id === id;

		$$invalidate(1, answerEvaluation = {
			correctAnswer: isCorrect,
			evaluation: isCorrect
			? questionEvaluation.correctAnswerRationale
			: questionEvaluation.incorrectAnswerRationale
		});
	};

	const click_handler = choice => addAnswer(choice.id);

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*$selectedQuestion*/ 1) {
			$: {
				$selectedQuestion;
				firstAnswer = true;
				$$invalidate(1, answerEvaluation = null);
			}
		}
	};

	return [$selectedQuestion, answerEvaluation, addAnswer, click_handler];
}

class Question extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Question;